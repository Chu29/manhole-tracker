name: Keep Render Backend Awake
on:
  schedule:
    - cron: '*/12 * * * *' # Runs every 12 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render app
        run: curl -s https://manhole-tracker-backend.onrender.com/api/health
